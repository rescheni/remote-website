server:
  addr: "your-server.com:8443"
  tls: false
  secret: ""

client:
  id: "my-phone"
  heartbeat_interval: "30s"
  reconnect_backoff:
    - "1s"
    - "2s"
    - "5s"
    - "10s"
    - "30s"

# Routes are optional - can also be managed via Dashboard
routes: []
